build/
build-*/
.DS_Store
*.o
*.dylib
compile_commands.json
.cache/
.vscode/

# Flutter / Dart
app/.dart_tool/
app/.flutter-plugins*
app/build/
app/macos/Flutter/ephemeral/
app/macos/Pods/
# app/pubspec.lock is deliberately NOT ignored: this is an application, so the
# resolved dependency set is part of what we ship, and the licence audit reads it.
.fvm/

# Test + capture artefacts
artifacts/
*.wav.actual
# Golden-failure debug images. Written by `flutter test` on a mismatch and
# useful for exactly as long as it takes to look at them.
app/test/failures/
